const path = require('path');

module.exports = {
  "config": path.resolve('./db/config', './db/config.json'),
  "models-path": path.resolve('./db/models'),
  "migrations-path": path.resolve('./db/migrations')
};